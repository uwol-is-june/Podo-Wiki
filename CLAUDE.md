@docs/AGENTS.md

# Podo Wiki

공연단체들끼리 인수인계 문서를 공유하는 위키 플랫폼.
나무위키 형태이나, 수정은 회원가입된 사용자만 가능.

- 배포: https://podo-wiki.vercel.app
- 할 일: [docs/TASK_W.md](docs/TASK_W.md) (웹) / [docs/TASK_M.md](docs/TASK_M.md) (앱) / [docs/TASK_A.md](docs/TASK_A.md) (공통)
- 아키텍처: [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) (구조가 바뀔 때만 업데이트)
- 앱 배포·버전 관리: [docs/APP-RELEASES.md](docs/APP-RELEASES.md) (스토어 제출 시마다 갱신)

## 구성

한 저장소에 **웹과 모바일 앱 두 개**가 들어있어. 작업 전에 어느 쪽인지 먼저 확인해:

- `src/` — Next.js 16 웹 (Vercel 배포)
- `mobile/` — Expo 네이티브 앱 (App Store / Play Store). 자체 `mobile/CLAUDE.md` 규칙이 따로 있음

### 웹 ↔ 모바일 동기화 주의

`mobile/src/theme/colors.ts`는 `src/app/globals.css`의 `--wiki-*` 팔레트를 **손으로 복사해둔 파일**이야. 자동 공유가 아니라서 한쪽만 고치면 다른 쪽은 그대로 남아. 색·디자인 토큰을 건드리면 **반드시 양쪽 다** 수정해.

## 코딩 규칙

- 언어: TypeScript (Node.js 24)
- 프레임워크: Next.js 16 (App Router), Tailwind CSS v4
- DB/Auth: Supabase
- 커밋: feat / fix / docs / refactor / chore 타입 prefix
- 색상은 하드코딩하지 말고 `--wiki-*` 토큰(웹) / `WikiColors`(모바일)를 써

## 태스크 관리 (세 파일로 분리 — TASK.md·BACKLOG.md는 폐지됨)

태스크는 성격에 따라 세 파일로 나눠 관리해:

| 파일 | 넣을 것 |
|---|---|
| [docs/TASK_W.md](docs/TASK_W.md) | **웹 전용** — `src/`만 고치면 끝나는 일 |
| [docs/TASK_M.md](docs/TASK_M.md) | **앱 전용** — `mobile/`만 고치면 끝나는 일 |
| [docs/TASK_A.md](docs/TASK_A.md) | **공통** — 웹·앱 양쪽에 같이 반영해야 의미가 있는 일 |

### 어느 파일에 넣을지는 네가 판단해

**사용자에게 "어디에 넣을까요?" 하고 되묻지 마.** 태스크를 적어달라고 하면 아래 절차로 스스로 분류해서 넣고, **결과만 "TASK_A.md에 넣었습니다"처럼 알려줘.** 분류가 틀렸으면 사용자가 고쳐달라고 할 테니 일단 판단해서 진행하는 게 맞아.

판단 기준은 **"고치는 코드가 어디 있냐"가 아니라 "어디까지 바뀌어야 이 일이 끝나냐"**야. 전자로 판단하면 웹 파일을 고치는 일이라는 이유로 TASK_W로 보내버리고 앱 반영이 통째로 누락됨 — 실제로 자주 하는 실수니 주의.

1. 사용자가 말한 증상·요구가 **앱에도 그대로 존재하는지 먼저 확인해.** 추측하지 말고 `mobile/` 쪽을 grep해서 같은 코드·패턴이 있는지 실제로 볼 것
2. 웹에만 있으면 TASK_W, 앱에만 있으면 TASK_M, **양쪽 다 있으면 TASK_A**
3. 확인했는데도 애매하면 TASK_A. 공통에 잘못 넣으면 한쪽 범위를 나중에 지우면 그만이지만, 전용 파일에 잘못 넣으면 반대쪽이 조용히 누락됨

특히 **디자인 토큰·색상은 거의 항상 공통**이야. `mobile/src/theme/colors.ts`가 웹 팔레트를 손으로 복사해둔 파일이라 한쪽만 고치면 반드시 어긋남. 그 외 양쪽에 같이 노출되는 문구·정책, 공통 UX 규칙도 공통.

- 공통 태스크는 **쪼개지 말고 하나로 두되**, 배경에 웹/앱 작업 범위를 나눠 적고 순서(보통 웹에서 값 확정 → 앱 적용)와 배포 시점 차이를 명시해. 앱은 스토어 심사를 거쳐야 사용자에게 도달함
- 웹·앱 양쪽에 걸친 일을 두 태스크로 쪼개면 "함께 적용해야 한다"는 정보가 두 파일로 흩어져서 약해져. 쪼개는 건 실제로 독립 배포가 가능하고 서로 몰라도 되는 경우에만
- **번호는 세 파일이 하나의 연속 번호를 공유해.** 파일별로 따로 세면 같은 번호가 여러 파일에 생겨서 CHANGELOG에서 구분이 안 됨. 새 번호를 딸 때 세 파일을 다 확인할 것
- 앱이 걸린 태스크(TASK_M·TASK_A)는 **릴리즈 영향을 배경에 적어.** 웹은 머지 즉시 반영되지만 앱은 스토어 심사를 거쳐야 사용자에게 도달함 (출시 이력 → [docs/APP-RELEASES.md](docs/APP-RELEASES.md))
- 작업 시작 전 반드시 해당 태스크 파일을 먼저 읽어

### 태스크 파일 구조와 포맷

세 파일 모두 `# 제목` 아래 **`## 진행 중` / `## 대기 중` / `## 구체화 필요`** 세 섹션만 둬. 규칙 설명이나 안내문은 이 CLAUDE.md에만 두고 **태스크 파일 안에는 적지 마** — 목록만 깔끔하게 보이는 게 목적이야. 비어 있는 섹션은 `_없음_`.

항목 포맷: `- [ ] [TASK-NNN] (O|S) 태스크 이름` + 다음 줄부터 `  → **배경**: ...` 식으로 들여쓴 상세 내용

- **모든 태스크에는 권장 모델과 그 이유를 반드시 함께 적어.** 예외 없음:
  - **(O)** = Opus — 설계 판단, 여러 파일에 걸친 구조 변경, 까다로운 디버깅
  - **(S)** = Sonnet — 구현 방법이 명확한 단순 구현/수정
  - 표기 위치: 태스크 ID 바로 뒤에 `(O)` / `(S)`
  - 이유: 배경 마지막 줄에 `→ **권장 모델**: Opus — 이유` 형태로 한 줄 추가. `(O)` 한 글자만으로는 왜 그 모델인지 알 수 없어서 판단 근거를 남겨야 해
  - 애매하면 (O). 잘못 고른 비용은 Opus 쪽이 더 싸
  - 예시:
    ```
    - [ ] [TASK-062] (O) 다크모드 보라색 글자 가독성 개선
      → 배경: ...
      → **권장 모델**: Opus — 토큰 구조 분리 설계 + 웹/모바일 50여 개 호출부 동시 변경
    ```
- 새 아이디어가 생기면 위 기준으로 고른 태스크 파일에 추가해:
  - 구현 방법이 명확하면 → **대기 중**에 추가 (모델 표기 (O|S) 포함)
  - 불명확하거나 결정이 필요하면 → **구체화 필요**에 추가하고 필요한 질문을 → 로 함께 작성해
  - 구체화 필요 항목의 모든 질문에 답변이 달리면 → 답변을 배경에 반영하고 모델 표기를 부여해서 **대기 중**으로 이동해
- 태스크 완료 시 해당 태스크 파일에서 삭제하고 docs/CHANGELOG.md Unreleased 섹션으로 이동해
