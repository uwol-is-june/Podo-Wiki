# Tasks — 앱 전용 (Expo / `mobile/`)

## 진행 중

_없음_

## 대기 중

- [ ] [TASK-082] (S) 앱에도 단체 로고 받침 적용 (다크모드 '흰 네모' 문제)
  → **배경**: 흰 배경 로고가 다크모드에서 뜬금없는 흰 네모로 보이는 문제. 웹은 TASK-081로 이미 적용됐고(CHANGELOG 참고), 앱만 남았다. 웹에서 눈으로 확인해 값이 확정되면 그대로 옮기면 됨
  → **적용할 값(웹 확정치)**: 받침색 라이트 `#ffffff` / 다크 `#f7f5fc`(= 라이트모드 배경색), 로고 칸 안쪽 여백은 칸 크기의 약 9%(웹은 88px 칸에 8px)
  → **손볼 곳**: `mobile/src/theme/colors.ts`에 `logoPlate` 토큰 추가(웹 `--wiki-logo-plate`를 손으로 복사하는 파일이라 값이 어긋나지 않게 주의) + `mobile/src/app/(tabs)/index.tsx`의 `troupeLogo`(68px, borderRadius 10)를 받침 View로 감싸고 `contentFit="contain"` 유지
  → **선행**: 웹 배포본을 다크모드로 보고 받침색·여백이 괜찮은지 확인 후 진행. 웹에서 값이 바뀌면 여기 값도 같이 고칠 것
  → **릴리즈 영향**: 스토어 심사를 거쳐야 사용자에게 도달함 — 다음 앱 빌드에 포함
  → **권장 모델**: Sonnet — 값이 이미 확정돼 있고 파일 두 개만 고치는 단순 적용

## 구체화 필요

_없음_
