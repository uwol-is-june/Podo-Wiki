# Tasks — 앱 전용 (Expo / `mobile/`)

## 진행 중

_없음_

## 대기 중

- [ ] [TASK-064] (S) Android **1.1.0** Play Store 배포 — 다크모드 개선 + 북마크 + 본문 크기 조정을 한 번에
  → **배경**: 아래 네 건을 **한 빌드로 묶어 1.1.0으로 출시**한다(2026-08-02 사용자 결정). 현재 스토어는 1.0.3(7/25 출시)
    - T-062·T-063 — 다크모드 대비 개선 ✅ 코드 반영 완료
    - T-065 — 북마크 탭 + 문서 화면 개편 ✅ 코드 반영 완료
    - T-067 — 본문·표 크기 조정 ✅ 코드 반영 완료
  → ✅ **선행 태스크 전부 완료** (2026-08-02)
  → ✅ **app.json 1.1.0 + 양대 EAS 빌드 완료** (2026-08-02) — Android vc 7 / iOS build 5
  → ✅ **iOS는 `eas submit`으로 ASC 업로드까지 완료**
  → ✅ 웹도 같은 날 배포 완료 (T-066 웹 북마크 포함)
  → ✅ **양대 스토어 심사 제출 완료** (2026-08-02) — Android Play Console 프로덕션, iOS ASC 심사 제출
  → ⏳ **현재 심사 대기 중.** Android 보통 몇 시간~2일, iOS 보통 1~2일
  → **심사 통과하면**: APP-RELEASES.md 두 표를 "심사 중" → "출시됨"으로 바꾸고 이 태스크를 CHANGELOG로 이동. 그때 이 태스크는 끝
  → **반려되면**: 사유를 배경에 적고 대응을 새 태스크로. iOS 심사 방어 논리는 APP-RELEASES.md "알아두기" 참고 (읽기 전용 · 로그인 없음 · 편집은 웹에서 승인 회원만)
  → **에뮬레이터 검증은 이미 한 차례 끝냈음** — 릴리스 빌드로 라이트/다크 양쪽에서 북마크 저장·재시작 유지·삭제, 접이식 목차, 맨 위로 FAB, 최근 변경 진입, 본문·표 크기, 각주 색까지 확인. 아래 체크리스트는 **버전을 올려 다시 빌드한 뒤 한 번 더** 훑는 용도
  → ⚠️ **빌드 명령에 `ANDROID_HOME` 필요**: `expo prebuild`가 `android/`를 재생성하면서 SDK 경로가 담긴 `local.properties`를 지운다. APP-RELEASES.md 부록 명령에 `ANDROID_HOME=/opt/homebrew/share/android-commandlinetools`를 함께 넘길 것
  → **버전**: 1.0.3 → **1.1.0** (기능 추가 레벨 — APP-RELEASES.md 버전 규칙). `mobile/app.json`의 `version` **한 곳만** 수정 — versionCode는 EAS가 자동 증가(`appVersionSource: remote` + `autoIncrement`), 직접 건드리지 말 것
  → **네이티브 의존성 추가 주의**: T-065에서 AsyncStorage가 들어오므로 **prebuild부터 다시** 해야 함. 기존 빌드 산출물 재사용 불가
  → **절차는 [APP-RELEASES.md](APP-RELEASES.md)의 "배포하는 법"을 그대로 따를 것.** 아래는 이번 건 특이사항만:
  → **에뮬레이터 검증이 이번엔 특히 중요함**: T-062·T-063은 **순수 시각 변경인데 아직 실제 화면을 아무도 안 봤음**(빌드·타입체크·대비율 계산만 통과). 1.0.0을 검증 없이 냈다가 전면 크래시 낸 전례도 있음. 릴리스 빌드로 설치한 뒤 **에뮬레이터를 다크모드로 전환**하고 아래를 눈으로 확인:
    - 홈 — 단체 이니셜 배지(보라 원 안 글자), 최근 문서 제목, FAQ `Q.`, "전체 보기"
    - 검색 · 최근 변경 — **목록 전체가 보라색 제목이라 체감이 가장 큰 화면**
    - 문서 상세 — 브레드크럼, 목차 시트
    - 탭바 — 선택된 탭의 아이콘과 라벨 색이 서로 어긋나지 않는지
    - 이력 — 버전 선택 체크마크(✓), "선택한 버전 비교" 버튼 글자
    - 더보기 → 기능 추가 요청 — 제출 버튼 글자, 제출 중 스피너
    - **라이트모드도 한 번 훑을 것** — 라이트는 변화가 없어야 정상(회귀 확인)
    - T-065 — 북마크 추가/삭제, 앱 재시작 후 유지, 목차 접이식, 맨 위로 FAB
    - T-067 — 본문·표 크기가 의도대로인지, 표가 가로로 잘리지 않는지
  → **빌드·제출**: `npx eas-cli build -p android --profile production` → AAB 다운로드 → Play Console 새 버전. Android는 서비스 계정 키(JSON)가 없어 **자동 제출 불가, 콘솔에서 수동 업로드**
    - "가독화 파일 없음" 경고가 뜨면 업로드 중단하고 원인 확인 (T-057에서 R8 mapping 자동 포함 설정됨)
  → **iOS는 이번에 제외** — 사용자가 Android만 요청. 다만 **같은 변경이 iOS에도 그대로 적용돼 있어** 미출시 상태로 남음. 버전 이름은 양 플랫폼이 공유하므로 다음에 iOS를 제출하면 1.1.0 이후 번호로 나감. iOS도 낼지는 별도 판단
  → **제출 후**: [APP-RELEASES.md](APP-RELEASES.md) 상단 두 표 갱신(①지금 버전 ②릴리스 이력) → 이 태스크 삭제하고 [CHANGELOG.md](CHANGELOG.md)로 이동 → 심사 통과하면 상태를 "심사 중" → "출시됨"으로
  → **권장 모델**: Sonnet — APP-RELEASES.md에 절차가 단계별로 문서화돼 있어 판단할 게 없음. 다만 에뮬레이터 다크모드 육안 검증만은 대충 넘기지 말 것

- [ ] [TASK-068] (S) 문서 화면 북마크 아이콘을 별(★) → 북마크 리본 모양으로 통일
  → **배경**: 문서 화면 헤더의 북마크 토글이 `★`/`☆` 텍스트 글리프인데, 같은 기능인 **탭바 아이콘은 북마크 리본**(`sf="bookmark.fill"` / `md="bookmark"`)이라 은유가 섞여 있음. 라벨도 "북마크"이므로 리본으로 맞추는 게 맞음. 웹은 이미 리본 SVG를 쓰고 있어서(`src/components/wiki/BookmarkButton.tsx`) 앱만 고치면 웹·앱·탭바가 전부 일치함
  → **고칠 곳**: `mobile/src/app/w/[slug].tsx`의 `headerRight` — `{isBookmarked ? '★' : '☆'}` 부분과 `styles.headerIconText`
  → **구현 방법 검토** (텍스트 글리프를 쓴 이유가 플랫폼 호환 때문이었음. 아래 중 택1):
    - **`expo-symbols`** — 이미 의존성에 있음. 다만 `SymbolView`는 iOS 전용이라 Android 폴백을 따로 줘야 함. Android가 주 배포 대상이라 폴백이 필수
    - **`@expo/vector-icons`** — 새 의존성이지만 Expo 표준이고 양 플랫폼 동일하게 렌더됨. `MaterialIcons`의 `bookmark`/`bookmark-border`가 탭바의 `md="bookmark"`와 같은 계열이라 가장 잘 맞음. **권장**
    - **인라인 SVG** (`react-native-svg`) — 웹의 리본 path를 그대로 재사용해 웹과 픽셀 단위로 일치시킬 수 있음. 의존성 추가 필요
  → **주의**: 채움/비움 두 상태가 명확히 구분돼야 함(저장됨 = 채운 리본). 헤더 배경이 `--wiki-header-bg`라 다크에서도 대비가 나오는지 확인할 것
  → **검증**: 에뮬레이터에서 라이트/다크 양쪽으로 헤더 아이콘과 탭바 아이콘이 같은 모양인지, 토글 시 채움/비움이 바뀌는지
  → **릴리즈 영향**: 순수 시각 변경. 1.1.0 심사 중이므로 **다음 버전(1.1.1)** 에 실릴 것. 새 의존성을 넣으면 prebuild부터 다시 빌드해야 함
  → **권장 모델**: Sonnet — 고칠 위치가 한 파일의 한 블록이고, 아이콘 라이브러리 선택지도 위에 정리돼 있음

- [ ] [TASK-071] (S) 문서 화면 잔손질 — 맨 위로 FAB 깜빡임, 불필요한 캐시 무효화
  → **배경**: 2026-08-02 코드리뷰에서 나온 낮은 우선순위 두 건. 동작이 깨지진 않지만 다듬을 값어치가 있음
  → **① FAB가 눌린 직후 다시 나타남** — `mobile/src/app/w/[slug].tsx:70-73, 155`
    - `scrollToTop`이 `behavior:'smooth'`로 스크롤을 걸고 곧바로 `setShowTopFab(false)` 함. 그런데 부드러운 스크롤이 진행되는 동안 WebView가 계속 `onScroll`을 쏘고 그때 `y`는 아직 400보다 커서 **바로 다음 이벤트에 다시 `true`가 됨**
    - 긴 문서에서 버튼을 누르면 사라졌다가 다시 나타나 애니메이션이 y=400을 지날 때까지 떠 있음
    - 방향: 프로그램에 의한 스크롤 중에는 노출 판단을 잠시 막거나(플래그), 맨 위 도달을 확인하고 끄기
  → **② 문서를 열 때마다 불필요한 캐시 무효화** — `mobile/src/hooks/use-bookmarks.ts:45-47`
    - `syncBookmarkTitle`은 북마크가 아니거나 제목이 그대로면 아무것도 안 하고 빠져나오는데(대부분의 경우), 훅은 그 뒤에 **무조건 `invalidateQueries`를 호출**함
    - `staleTime: 0`이라 문서를 열 때마다 AsyncStorage를 한 번 더 읽고 화면·헤더가 다시 렌더됨. 루프는 아니고 낭비
    - 방향: 실제로 제목을 바꿨을 때만 무효화하도록 `syncBookmarkTitle`이 변경 여부를 돌려주게 할 것
  → **참고 (고칠 필요 없음)**: 스크롤마다 `setState`를 부르는 건 값이 같으면 React가 렌더를 건너뛰므로 문제가 아님. 다만 y=400 경계를 오갈 때는 헤더 옵션까지 다시 만들어지므로 ①을 고치면 이 부분도 같이 줄어듦
  → **릴리즈 영향**: 1.1.0 심사 중이므로 다음 버전
  → **권장 모델**: Sonnet — 두 건 다 원인과 위치가 특정돼 있음

## 구체화 필요

_없음_
