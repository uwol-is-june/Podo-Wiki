# Backlog

> 포맷: `- [ ] [BACK-NNN] 태스크 이름 → 배경/이유`
> TASK.md로 이동할 때 번호를 TASK-NNN으로 재부여

---

## High-fi
> 지금 바로 TASK.md로 옮겨서 구현 가능한 항목

- [ ] [BACK-002] 어드민 전체 사용자 목록 조회 → 현재 대기 중(pending)만 보이는 어드민 대시보드에 승인/거절된 사용자까지 포함한 전체 목록 탭 또는 섹션 추가
  - 배경: `getPendingProfiles()`는 status='pending'만 조회함. 전체 사용자(approved/rejected 포함) 조회용 `getAllProfiles()` 서버 액션 추가 필요.
  - 표시 항목: 이름, 소속, 상태(pending/approved/rejected), 가입일
  - 상태 필터(전체/대기/승인/거절) 제공

- [ ] [BACK-001] 열람 페이지에서 상위 페이지 탐색 → 슬러그가 `A/B/C` 형태일 때 페이지 상단에 브레드크럼(`A > B > C`)으로 전체 경로 표기
  - 배경: 현재 슬러그가 `/`로 계층 구조를 표현하지만 열람 화면에서 상위 페이지로 이동하는 수단이 없음. DB 변경 없이 슬러그 파싱만으로 구현 가능.
  - 상위 페이지 문서가 실제로 존재하지 않으면 해당 세그먼트는 브레드크럼에 표시하지 않음 (존재하는 상위 페이지만 링크로 표시)
  - 최상위 페이지(슬러그에 `/` 없음)는 브레드크럼 미표시

---

## Low-fi
> 추가적인 구체화가 필요한 항목
> 아래 질문의 답변이 달리면 High-fi로 이동

_없음_

---

## 우선순위 규칙

| 상황 | 행동 |
|------|------|
| 새 아이디어 | 구현 방법이 명확하면 → High-fi, 불명확하면 → Low-fi (질문 함께 작성) |
| Low-fi 질문 답변 완료 | 해당 항목을 High-fi로 이동, 답변 내용을 배경에 반영 |
| High-fi 항목 착수 | TASK.md로 이동 후 번호를 TASK-NNN으로 재부여 |
